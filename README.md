# Weekender pre-launch site

Static, hand-coded pre-launch site for Weekender.

## Files

- `index.html` — home page with hero email capture and launch teaser
- `about.html` — about page adapted from the supplied reference page
- `drop.html` — first drop preview with product-photo placeholder
- `contact.html` — contact and social placeholder page
- `styles.css` — shared design system and layout styles
- `assets/weekender-monogram.jpeg` — primary monogram used in the header and footer
- `assets/weekender-flower.jpeg` — secondary mark reserved for future use
- `assets/weekender-prototype-polos.png` — prototype polo placement image used on `drop.html`
- `assets/` — owner-supplied product photography and future static assets

## TODO before launch

- Replace every newsletter stub form with the chosen provider embed: Beehiiv, ConvertKit, or Mailchimp.
- Add final launch date or use final approved "soon" language on `drop.html`.
- Add real email address and social handles on `contact.html` and in the footer.
- Add owner-supplied logo and product photography to `assets/`.
- Optionally add `CNAME`, favicon, Open Graph image, `sitemap.xml`, and `robots.txt` when the domain is final.

No backend, cart, product catalog, accounts, or build tooling is included.
